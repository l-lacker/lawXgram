# Assets

- Treat most files here as data packs, not source code.
- Avoid renaming files without searching for runtime references first.
- Large groups include themes (`*.attheme`), fonts, emoji, models, shaders, and OCR or format data.
- Regenerate or replace assets only when the task explicitly requires it.
- Keep binary outputs minimal and consistent with existing naming and layout.
